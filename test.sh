#!/bin/sh

set -e

checkov --version
go version   
markdown-link-check --help       
pip3 --version
pre-commit --version
python3 --version
terraform --version
tflint --version          
tfsec --version
